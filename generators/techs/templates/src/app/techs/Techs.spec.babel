import Techs from './Techs.vue';

describe('Techs', () => {
  it('should be a techs', () => {
    expect(Techs.name).toEqual('Techs');
  });

  it('should return []', () => {
    expect(Techs.data().techs).toEqual([]);
  });
});
