import Tech from './Tech.vue';

describe('Tech', () => {
  it('should be a tech', () => {
    expect(Tech.name).toEqual('Tech');
  });

  it(`should return ['tech']`, () => {
    expect(Tech.props).toEqual(['tech']);
  });
});
